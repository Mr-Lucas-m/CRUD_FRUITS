# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.0

# Database
sqlalchemy==2.0.40
alembic==1.15.2
psycopg2-binary==2.9.10   # PostgreSQL driver (troque por asyncpg para uso async)

# Settings e validação
pydantic==2.11.1
pydantic-settings==2.8.1
python-dotenv==1.1.0

# Testes
pytest==8.3.5
pytest-asyncio==0.26.0
httpx==0.28.1            # cliente async para testes de integração

# Dev / qualidade
ruff==0.11.3             # linter + formatter ultrarrápido
